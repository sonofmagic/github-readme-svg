const { renderIcon } = require('./icon')
const { renderQRCode } = require('./qrcode')
const { renderRoad } = require('./road')
const { renderCaptcha } = require('./captcha')
const { renderFortawesomeIcon } = require('./fortawesome')
const { renderMiniprogram } = require('./mp')
const { renderMathjax } = require('./mathjax')
module.exports = {
  renderIcon,
  renderQRCode,
  renderRoad,
  renderCaptcha,
  renderFortawesomeIcon,
  renderMiniprogram,
  renderMathjax,
}
